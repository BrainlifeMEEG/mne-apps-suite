# Strategy — Wakeman & Henson (ds000117) reproduction on Brainlife.io

Phase 0 deliverable for the brief in `Claude prompt repro Wakeman.md`. Scope: map the
target pipeline (mne-biomag-group-demo / Frontiers 2018) onto this repo's Brainlife Apps,
flag what needs fixing or building, and list open questions — before anything runs.

## Status

- **Data**: confirmed already staged in the target project (`brainlife.io/project/5df7efdc32bff02262e226b6`) — raw MEG (`task-facerecognition`, `run-##`), MaxFilter derivatives (`proc-sss`, `run-##`), T1 (`acq-mprage`). Ready for Phase 1.
- **Source-space work (Figs 8–12)**: deferred. The `obVdo` apps this needs aren't in this repo yet, and you're contacting the developer directly before that work starts. Not analyzed further below.
- **⚠️ Subject-numbering: the paper's "subject 10" is BIDS sub-09, not sub-10** (found 2026-08-20, while building Fig 2 — full account in the open questions below). ds000117 has four subject-numbering schemes in circulation (original Wakeman & Henson 2015, openfMRI, an FTP N=16 subset, and BIDS/OpenNeuro), documented with an authoritative crosswalk table in the dataset's own README, and they are **not** a constant offset of each other. The mne-biomag-group-demo scripts (and so the paper) use their own internal numbering where "subject 10" = BIDS sub-09. Confirmed empirically: sub-09's demo-repo bad-channel list for run 2 (7 channels) reproduces the paper's actual published Figure 2 closely; BIDS sub-10 has zero marked bad channels in any run and doesn't match the published figure at all. **Both subjects are now built out** (see below) — sub-09 for paper-figure-matching, sub-10 kept as already-valid platform work. Figures going forward default to sub-09 to match the paper; pass `--subject 10` to any figure script to regenerate the sub-10 version instead.
- **Phase 1 (fif2mne) and Phase 2 (maxwell-filter, Fig 1)**: done for both S09 and S10, run02 (S09 also has run04, needed for Fig 1's cross-run alignment — see below). See `run_pipeline.py --subjects 09,10 --runs 02` and `figures/fig1_maxfilter_comparison.py --subject <09|10>`. Along the way: fixed a real `maxwell-filter` bug (`head_pos` fed the wrong file type) and a real `bl-app-run-instanced.js` crash (in `BrainlifePipelineCLI`). Fig 1 is a butterfly plot (per-sensor traces, colored by sensor position), 1–40 Hz filtered, -200 to 800 ms, matching the paper's actual caption — confirmed directly against the published `03-maxwell_filtering.py` source that there's no `mne.chpi.filter_chpi()` call in the real pipeline (an earlier draft used it and got a superficially clean result for the wrong reason, see `fig_chpi_diagnostic.py`); the real recipe is `calibration`/`cross_talk`/`st_duration` (tSSS)/`origin`/`destination`/`head_pos` in `maxwell_filter()`, bad channels read from a MaxFilter log file, then a plain 1–40 Hz post-filter. We supply calibration/cross_talk + explicit `origin` (matching Elekta's documented value, not MNE's `origin="auto"` fit, which converges substantially off) + `destination` (cross-run alignment to run 4, matching Elekta's own `trans=run4`) — panel C (MNE − Elekta difference) is now essentially flat for both subjects tested. tSSS/head_pos/exact autobad still not reproduced, see open questions.
- **Phase 3 (PSD, Fig 2)**: done for S09 (paper-matching) via `figures/fig2_psd.py` — EEG channels only (not MEG), raw/pre-SSS data, bad channels from the demo repo's per-subject list. The `psd`/`epoch-psd` Brainlife apps are still broken (see below) — this figure bypasses them entirely with a local script, consistent with the local-scripts-first approach.
- **Raw upload data gap**: this project's raw MEG datasets have no `calibration`/`crosstalk`/`channels` sidecar files attached (all resolve to nonexistent paths) — only `events.tsv`/`headshape.pos`/`coordsystem.json` came through. Calibration/crosstalk turned out to matter less than feared for Fig 1 (see below), but this is still worth fixing via the maintainer conversation already in progress.

## Figure-by-figure roadmap

Cross-referenced against the actual mne-biomag-group-demo script order and the Frontiers 2018 paper's figure captions (both fetched directly) — the brief's figure numbers line up with the paper almost exactly.

| Figure | Content | Target script | Brainlife App(s) | Status |
|---|---|---|---|---|
| — (read data) | — | `02-extract_events.py` | `fif2mne` | ✅ exists |
| 1 | raw vs. MNE maxwell_filter vs. Elekta MaxFilter difference | `03-maxwell_filtering.py` | `maxwell-filter` | ✅ **done for S09 (paper-matching) and S10, close match both** — `figures/fig1_maxfilter_comparison.py --subject <09\|10>`, butterfly plot matching the paper's actual A/B/C structure and caption (1–40 Hz, -200 to 800 ms). Uses a local SSS run (calibration/cross_talk + Elekta's exact origin + destination aligned to run 4, no app) since the brainlife.io app has no wiring for these shared/cross-run inputs yet. Panel C (MNE − Elekta) is essentially flat for both subjects. Two fixes got there: explicit `origin` matching Elekta's documented value per-subject (MNE's `origin="auto"` fit converges substantially off) and `destination=<run4's raw>` (cross-run alignment, matching Elekta's `trans=run4`). Movement compensation and MNE's auto-bad-channel detection were tested and don't help |
| 2 | PSD, subject 10 (paper) = BIDS sub-09, run 02 | (pre-ICA QC) | `psd` / `epoch-psd` | ✅ **done via local script** — `figures/fig2_psd.py`, EEG-only PSD (log + linear scale) with bad channels colored red, matches the paper's published figure closely (including a real floating-electrode gotcha, EEG064, fixed after user review). The `psd`/`epoch-psd` apps themselves: **correction (2026-08-20) — not actually broken.** Both were already migrated to `compute_psd()` on 2026-07-16, before this project started; the "broken, uses deprecated psd_welch/psd_multitaper" claim here was stale info carried over from `REMEDIATION_SUMMARY.md` without re-checking current code. Verified by actually running both end-to-end against real S09 data (raw for `psd`, a small real epochs set for `epoch-psd`) — both exit 0 and produce correct TSV + PNG + product.json output. Nothing to fix |
| 3 | filter frequency/impulse response, old MNE 0.12 vs. current | `04-python_filtering.py` | `filter-raw` | ✅ **done, full old-vs-new comparison** — `figures/fig3_filter_response.py`. Same A/B/C/D layout and blue(old)/orange(new) color scheme as the paper. Filter design confirmed against the actual script: `l_freq=1, h_freq=40`; current MNE built via `mne.filter.create_filter(..., trans_bandwidth='auto', filter_length='auto', ...)`. Old MNE 0.12's curves needed a real MNE 0.12 install (not installable alongside modern MNE in this env) — `figures/compute_old_mne_012_filters.py` documents the one-time isolated-venv setup (numpy/scipy compatibility shims: `np.float` alias, a `numpy.testing.dec` stub) and computes both filters' impulse responses by filtering a unit impulse (equivalent to the filter kernel for any LTI filter), using old MNE's actual defaults (`filter_length='10s'`, `trans_bandwidth=0.5`Hz, vs modern's adaptive `'auto'`) — precomputed once, saved as `.npy` under `figures/old_mne_012_reference/`, committed (small, ~750KB, expensive to reproduce). Result matches the paper's pattern closely: old MNE steeper cutoff but more extended ringing, current MNE gentler transition but more localized ringing |
| — (bad seg/chan, ICA, epoching) | new diagnostic figures, open-ended | `05-run_ica.py`, `06-make_epochs.py` | `mark-bad-raw`, `ICA-fit`, `ICA-apply`, `epoch`, `drop-bad-epo` | ⚠️ **WIP, two branches, neither fully resolved (2026-08-27)** — see `~/.claude/plans/jazzy-swinging-whistle.md` and `original_scripts/GLITCHES.md` for full detail. Methodology confirmed by literally downloading and reading the actual scripts (`original_scripts/`, all 19 + config): the **official main pipeline never recomputes Maxwell filtering** — it uses Elekta's deposited proc-sss file as-is (`run_%02d_sss.fif` = our `elekta_maxfilter_meg.fif`) and just filters on top; `03-maxwell_filtering.py`'s from-scratch tSSS recompute is called `subject_id=3` **only**, feeding Figure 4 (Phase 6), never the main chain. **Decided: default to Elekta's files, matching official methodology.** Current state: (1) `figures/fig5_diagnostics.py` — Elekta-file-based (the correct main line), extended to the full 6-run concatenation (fastica MEG-only, ctps ECG, EOG061-pinned zscore EOG — EOG062 confirmed broken for this subject throughout, not a fluke). CTPS now cleanly finds one ECG component (vs. single-run's noisy multi-candidate mess — confirms CTPS just needed more data). Bad-channel handling still incomplete: only marks MEG1611 bad (from manual pulse-detection on run02), missing MEG1031/1111/1933/2032 that Elekta's own MaxFilter log shows as autobad-detected per run — not yet re-tested with the correct list, so its before/after ICA result (still weak, ~-4% RMS) may improve once fixed. (2) `figures/fig5_pipeline.py` — a from-scratch Maxwell-filter recompute (not the main line, kept as an explicit future comparison branch): shows a genuinely convincing before/after (~1000fT→~230fT ECG-locked peak) and confirmed a separate "on-the-second" common-mode artifact present in Elekta's files (no STI/CHPI correlate found) vanishes entirely when SSS is recomputed locally — likely an Elekta-MaxFilter-internal artifact. Two real bugs found and fixed along the way (both still apply): `create_eog_epochs`'s auto channel selection picks the broken EOG062 over EOG061 — pin explicitly; `ica.apply()` on already-`(None,None)`-baselined epochs inflates output ~25x — apply ICA before baselining. ⚠️ `ICA-plot`/`ICA-fit`'s `main` bash script overwrites the correct `product.json` with a stale base64 heredoc (known bug) — fix opportunistically when we get to hardening these apps, not blocking |
| 4 | fanning artifact: baseline-only vs. 1Hz-HP vs. tSSS, subject 3 | `07-make_evoked.py` | `apply-baseline` | ✅ exists; figure needs a local script pulling 3 differently-processed branches together side by side. ⚠️ **"subject 3" needs the same numbering check as Fig 1/2 before starting** — verify against ds000117's README crosswalk / the demo's `map_subjects` dict which BIDS sub-NN this actually is, don't assume BIDS sub-03 |
| 5 | evoked response, subject 3 then all subjects | `07-make_evoked.py`, `11-group_average_sensors.py` | `average-erp` / `evoked-averaged` | ✅ exist for within-subject averaging. No app grand-averages Evoked across subjects yet — needed for the "all subjects" pass of this figure |
| 6, 7 | sensor cluster stats + decoding curve; spatiotemporal cluster | `11-group_average_sensors.py` | — | ❌ no app exists for evoked contrasts or cluster-permutation stats anywhere in the repo |
| decoding figure | sliding-window logistic regression, ROC-AUC | `10-sliding_estimator.py` | — | ❌ no app exists. Fig 6B already contains a decoding curve — worth checking whether the brief's separate "decoding figure" step is meant to extend that or is redundant, once we're there |
| 8–12 | BEM, coreg, whitening, group source, source stats | `01-anatomy.py`, `08-make_cov.py`, `12`–`16` | `obVdo`'s apps (not cloned) | **Deferred**, see Status above |

## Known app bugs relevant to this pass

- ~~`psd` / `epoch-psd` — deprecated MNE API calls~~ — **corrected 2026-08-20: not actually broken**, already fixed 2026-07-16 (before this project started). Verified by running both end-to-end against real data.
- `ICA-plot` / `ICA-fit` — `main` bash script clobbers a correctly-built `product.json` with a stale base64 heredoc. Cosmetic (affects Phase 5 diagnostics reports only); fix when we reach that phase.
- `average-erp`'s earlier config-key mismatch (`average-all` vs `average_all`) — already fixed, confirmed via `REMEDIATION_SUMMARY.md`. No action needed.
- `filter-epo` — not analyzed this pass; not on the critical path we're planning right now.

## Recommendation: local scripts first, promote to Apps later

For everything that doesn't have an app yet (the figure-recreation scripts, and eventually grand-averaging/contrast-stats/decoding), develop as **local Python scripts running inside the same `brainlifemeeg/mne:1.12.1` Docker image** apps run in, and only promote a stage to a compliant Brainlife App once its logic is validated and stable.

- The Docker image is already identical to what a real App would run in, so porting later is cheap — wrap already-working logic in `main.py`'s boilerplate (`load_config`, output-dir creation, `product.json` accumulator), not a rewrite.
- mne-biomag-group-demo itself is a set of plain scripts, not packaged apps — validating step-by-step against it is easier working the same way, especially for figures 2–7, which are exploratory comparison plots (e.g. Fig 4's 3-way baseline/highpass/tSSS comparison) pulling multiple branches together — awkward to force into a single App's one-shot input→output shape on a first attempt.
- Platform round-trips (task queueing, documented Amaretti archiving flakiness, `bl app wait` unreliability) add friction better avoided while a stage's logic is still being worked out.

## Reusable tooling

A new standalone tool, `local_scripts/BrainlifePipelineCLI/` (its own git repo, committed), now holds the generic engine extracted from `local_scripts/ReproduceProject/`'s prior (unrelated) reproduction work: retry/checkpointed app-run chaining (`PipelineRunner`), the tolerant `bl` JSON parser, and the Pipeline-tab rule-management library — all project-agnostic. A Wakeman-specific driver script (app ids, subject list, per-app configs) will be written against this package when Phase 1 execution starts, following the pattern in `BrainlifePipelineCLI/examples/example_driver.py`.

## Open questions

0. **Subject-numbering crosswalk, needed for every remaining figure that names a specific subject.** ds000117's own README (`https://github.com/OpenNeuroDatasets/ds000117/blob/master/README`) has the authoritative table across 4 numbering schemes: original Wakeman & Henson 2015 (`subject_NN`, N=19, 3 excluded), openfMRI (`subNNN`), an FTP N=16 subset (`SubNN`), and BIDS/OpenNeuro (`sub-NN`) — none related by a constant offset. The mne-biomag-group-demo repo's own `config.py` has a `map_subjects` dict (key = the paper's internal subject index, value = W&H `subject_NN` name) plus `exclude_subjects = [1, 5, 16]`. Worked example: paper's "subject 10" = `map_subjects[10]` = W&H `subject_12` = BIDS **sub-09** (per the README crosswalk table) — confirmed by matching bad-channel-list content against the paper's actual published Fig 2. Do NOT assume paper subject N = BIDS sub-N, and do NOT assume paper subject N = demo repo's literal `subject_N` folder (that's a false-friend naming coincidence — e.g. "subject_10" the folder is actually BIDS sub-07). Before trusting a subject identity for any new figure, cross-check via `map_subjects` + the README table, the way Fig 1/2 did.
1. Fig 6B already contains a decoding curve — is the brief's separate "decoding figure" phase meant to extend that, or produce something distinct?
2. Grand-averaging across subjects (needed starting at Fig 5's "all subjects" pass) — local script first per the recommendation above, confirm before Phase 7.
3. Source-space apps (`obVdo`) — on hold pending your contact with the developer; revisit scope once that's resolved.
4. Panel C (MNE − Elekta difference) in our Fig 1 is now essentially flat (evoked diff std 10.0 fT vs. Elekta's own 56.3 fT) after matching Elekta's origin and cross-run destination (run 4). Remaining small residual likely reflects `hpisubt=amp`/`linefreq=50`/`autobad=on` (documented in ds000117's derivatives JSON), none of which have an obvious direct MNE equivalent tried yet — worth chasing further, or is this close enough?
5. Should `maxwell-filter` (the app) be hardened with the fuller documented recipe (`calibration`/`cross_talk`/explicit `origin`/cross-run `destination`, all already declared in its config schema, all confirmed to matter for S10 run02) rather than running with defaults? `origin="auto"` converged ~30mm off from Elekta's own fit here — worth checking whether that's a one-off or systematic across subjects.
6. This fix needed run 4's data fetched (via a fresh `fif2mne` run) purely to extract its head position for `destination` alignment — worth doing this systematically (once per subject, reused across that subject's runs) once we move past S10 run02, rather than re-deriving per figure.
7. **(2026-08-27)** `fig5_diagnostics.py`'s bad-channel list is incomplete — only MEG1611 (manual pulse-detection), missing MEG1031/1111/1933/2032 that Elekta's own MaxFilter logs show as autobad-detected per run (source: the local full `ds000117` derivatives copy's `*_proc-sss_log.txt` files, `Static bad channels` lines — same mechanism `03-maxwell_filtering.py` itself uses). Not yet fixed; blocks trusting the Elekta-file-based branch's ICA result either way until re-tested.
8. **(2026-08-27)** The separate "on-the-second" common-mode artifact found in Elekta's proc-sss files (present in most of S09's 6 runs, no STI/CHPI correlate) vanishes entirely when Maxwell filtering is recomputed locally (`fig5_pipeline.py`) — mechanism still unconfirmed (best guess: something internal to Elekta's own MaxFilter 2.2.10 run, not physiological). Worth understanding before deciding whether Elekta's-files-as-is is safe to trust at scale, or whether this needs checking per-subject.

## Next step

Phase 5 is WIP, not done — see the roadmap row above and `~/.claude/plans/jazzy-swinging-whistle.md`.
Immediate next: re-run `fig5_diagnostics.py` (Elekta-file-based main line) with the correct
per-run bad-channel list parsed from Elekta's MaxFilter logs (matching `03-maxwell_filtering.py`'s
own mechanism — MEG1031/1111/1611/1933/2032, varying by run, not just MEG1611 globally) before
concluding whether Elekta's original files, properly bad-channel-corrected, already produce a
convincing ICA result on their own — this is the real open question, not yet settled. Only after
that: Phase 6 per the brief (baseline correction, catch up whole processing for subject 3 — ⚠️
verify BIDS identity first, same crosswalk check as S09/S10 got, see open question 0 — create
Figure 4, which is also where `03`'s tSSS recompute gets used as originally intended).
